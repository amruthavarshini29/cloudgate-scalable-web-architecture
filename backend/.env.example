# CloudGate Backend Environment Variables

# Server
PORT=8080
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# Database (PostgreSQL / RDS)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cloudgate
DB_USER=postgres
DB_PASSWORD=postgres
DB_SSL=false

# AWS (for deployment)
AWS_REGION=us-east-1
